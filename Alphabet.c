#include<stdio.h>
#include<conio.h>
int main()
{
    char a;
    printf("Enter the character");
    scanf("%c",&a);
    if(ch>='a' && ch<='z') || (ch>='A' && ch<='Z')
    {
        printf("Inserted character %c is an alphabet",a);
    }
    else()
    {
        printf("Inserted character %c is not alphabet",a);
    }
    return 0;
    
}